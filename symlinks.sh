#!/usr/bin/bash

ln -s $(realpath ./nvim) ~/.config/nvim
ln -s $(realpath wezterm) ~/.config/wezterm
ln -s $(realpath .fzf) ~/.fzf
ln -s $(realpath .zshrc) ~/.zshrc
ln -s $(realpath .zprofile) ~/.zprofile
ln -s $(realpath .zshrc.zni) ~/zshrc.zni
ln -s $(realpath .p10k.zsh) ~/.p10k.zsh
